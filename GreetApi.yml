server:
  port: 2222

spring:
  boot:
    admin:
      client:
        url: http://localhost:9090

management:
  endpoints:
    web:
      exposure:
        include: "*"

msg: Hello from Config Server to GreetApi
